<script type="text/javascript">
$(document).ready(function()
{ 
      	var url = "/lire/feeds/front_page/";
	    $.get(url, function(html) {
	        $("#random-feeds").html(html);
	    });
    
        $(".feed-link").each(function () {
          $(this).click(function() {
            var id = this.id;
            var arrid = id.split('-');
            id = arrid[2];
            
            var url = "/lire/feeds/"+id;
            $.get(url, function(html) {
	          $("#random-feeds").html(html);
	        }); 
          });
        });
});
//feed_path(feed)
</script>
<div id="feeds-list" class="feeds-list">
<h1>Lire Feeds</h1>
<table>
    <% @feeds.each do |feed| %>
    <tr>
      <td class="feed-list-item"><%= link_to feed.name, '#', :id => "feed-link-#{feed.id}", :class => 'feed-link' %></td>
      <!--<td><%= link_to "Edit", edit_feed_path(feed), :class => 'btn'  %></td>
      <td><%= link_to 'Delete', feed, :confirm => 'Sure?...He no le muevas si no eres Hector... aun no le pongo seguridad a esto', :method => :delete, :class => 'btn' %></td>-->
    </tr>
    <% end %>
</table>
</div>
<<<<<<< HEAD
<!-- <div id='random-feeds' class="random-feeds" style="overflow: auto; width: 78%%; height: 1000px;"> -->
<div id='random-feeds' class="random-feeds">
=======
<div id='random-feeds'>
    <div class="feed">
        <% i=0 %>
        <% @mem_feeds.each do |item| %>
          <div class="feed_item" id="feed_item_<%= i %>">
            <div class="feed_item_title"><a href="<%= item.url %>" target="_blank"><%= item.title.html_safe %></a></div>
            <div class="feed_item_date"><%= item.published if defined? item.published %></div>
          </div>  
        <% end %>
    </div>
>>>>>>> 81ba359a37b94844b6f7fee7422a02d276ab7c83
</div>
<br/><br/>
